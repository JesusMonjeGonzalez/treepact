"""Ports defined by TreePact: ModelProvider and RuntimeAdapter protocols.

Ownership boundary: ports are interfaces owned by the TreePact domain;
adapters implement them. The domain never imports an adapter.
"""
