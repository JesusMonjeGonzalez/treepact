"""Workspace supervision: Git discovery, detached worktrees, path broker,
tool implementations, sanitized environments, and process groups.

Ownership boundary: the model-facing surface exposes only typed tools;
no raw process or Git tool is ever exposed to a runtime.
"""
